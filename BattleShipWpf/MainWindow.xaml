<Window x:Class="BattleShipWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BattleShipWpf"
        mc:Ignorable="d"
        FontSize="24"
        Title="Морской Бой" Height="450" Width="800">
  
  <Grid>
    
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="2*" />
      <ColumnDefinition Width="2*" />
    </Grid.ColumnDefinitions>

    <StackPanel Margin="20">
      <TextBlock d:Text="0:00"
                 Text="{Binding Time}"
                 FontSize="36"
                 HorizontalAlignment="Center" 
                 Margin="0, 0, 0, 10"/>
      <TextBlock Text="{Binding Steps}" d:Text="Step: 5"/>
      <TextBlock Text="{Binding Goal}" d:Text="Goal: 5"/>
    </StackPanel>

    <Button Grid.Column="1" Content=" Start " Click="btnStart" />
    <Button Grid.Column="2" Content=" Stop " Click="btnStop" />
  </Grid>
</Window>
